---
import MainLayout from "../layouts/MainLayout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";

const pageTitle = "Inicio - Organizadora de Eventos";
const pageDescription = "Creamos eventos inolvidables en Tuxtla Gutiérrez, Chiapas. Bodas, fiestas, corporativos y más.";
---
<MainLayout title={pageTitle} description={pageDescription}>
  <Navbar slot="navbar" />

  <!-- Hero -->
  <section class="relative h-[500px] flex items-center justify-center bg-cover bg-center animate-fadeIn" style="background-image: url('/img/hero.webp');">
    <div class="bg-black bg-opacity-50 p-8 rounded-lg text-center">
      <h1 class="text-4xl md:text-5xl font-bold text-white">Creamos momentos únicos</h1>
      <a href="/contacto" class="mt-6 inline-block bg-red-500 text-white px-6 py-3 rounded-lg hover:bg-red-600 transition">Contáctanos</a>
    </div>
  </section>

  <!-- Galería -->
  <section class="max-w-7xl mx-auto px-4 py-12 animate-slideUp">
    <h2 class="text-2xl font-bold mb-6 text-center">Galería de eventos</h2>
    <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
      {Array.from({ length: 10 }, (_, i) => (
        <img loading="lazy" src={`/img/galeria${i+1}.webp`} alt={`Evento ${i+1}`} class="rounded-lg shadow-lg hover:scale-105 transition" />
      ))}
    </div>
  </section>

  <!-- Servicios rápidos -->
  <section class="bg-white py-10 animate-fadeIn">
    <div class="max-w-7xl mx-auto px-4 grid md:grid-cols-3 gap-8">
      <div class="p-6 rounded shadow hover:shadow-lg transition transform hover:-translate-y-1">
        <h3 class="text-lg font-bold text-red-500">Bodas</h3>
        <p>Planeación completa para el día más importante de tu vida.</p>
      </div>
      <div class="p-6 rounded shadow hover:shadow-lg transition transform hover:-translate-y-1">
        <h3 class="text-lg font-bold text-red-500">Eventos corporativos</h3>
        <p>Organización profesional de conferencias y lanzamientos.</p>
      </div>
      <div class="p-6 rounded shadow hover:shadow-lg transition transform hover:-translate-y-1">
        <h3 class="text-lg font-bold text-red-500">Celebraciones</h3>
        <p>Cumpleaños, aniversarios y fiestas privadas a medida.</p>
      </div>
    </div>
  </section>

  <!-- Clientes -->
  <section class="max-w-7xl mx-auto px-4 py-12 animate-slideUp">
    <h2 class="text-2xl font-bold mb-6 text-center">Clientes que confían en nosotros</h2>
    <p class="mb-8 text-center">Estas son algunas de las marcas y personas que han depositado su confianza en nuestro equipo para crear experiencias inolvidables.</p>
    <div class="flex justify-between items-center flex-wrap gap-6">
      {Array.from({ length: 6 }, (_, i) => (
        <img src={`/img/logo${i+1}.webp`} alt={`Cliente ${i+1}`} class="w-24 h-24 grayscale hover:grayscale-0 transition" />
      ))}
    </div>
  </section>

  <!-- Contacto rápido -->
  <section class="bg-red-500 text-white py-12 text-center animate-fadeIn">
    <h2 class="text-2xl font-bold mb-4">¿Listo para comenzar?</h2>
    <p class="mb-6">Haz realidad tu evento soñado. ¡Estamos a un mensaje de distancia!</p>
    <a href="/contacto" class="bg-white text-red-500 px-6 py-3 rounded-lg font-semibold hover:bg-gray-100 transition">Contáctanos ahora</a>
  </section>

  <Footer slot="footer" />
</MainLayout>
